function data_obj = import_onsets(data_obj, design_file)
% Import onsets from a csv or Excel file and attach them to the fmri_glm_design_matrix object
            
            % DesginmatrixTable INPUT
            % TRY readtable() -- may be more flexible than xlsread.
            xlsread(DesginmatrixTable)


end

