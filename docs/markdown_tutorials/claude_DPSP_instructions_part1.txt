Build a tutorial that will go through some of the basic options for classification and cross-classification with CANlab tools and SVM. This should be a markdown file with explanation, links, code, and embedded figures showing key results. Store the markdown file in docs/markdown_tutorials/multivariate_classification_with_SVM in a file called multivariate_classification_with_SVM.md.  At the end, after the markdown file has been created, turn the whole thing into a live script (.mlx) as well and link to it in the markdown. But that is for later, we will go step by step.

The tutorial does the following steps below and is formatted and commented in a way that will render nicely when published.  It uses the sample data in DPSP_pain_rejection_participant_maps in CanlabCore.  It can incorporate information from the tutorials page
refer to and link to 2015_Woo_NatureComms_Rejection in Neuroimaging_Pattern_Masks github repo
and the PDF Woo_2014_NatComms_dpSP_romantic_rejection.pdf.  When you run the Matlab code, save figures in a pngs/ subfolder docs/markdown_tutorials/multivariate_classification_with_SVM/pngs and embed these into the .md file so we can see the output.

0. Explain a little bit about the study.  Be concise, 1 paragraph describing the design mainly and the idea of classification and cross-classification, both at the whole-brain and within-region multivariate pattern level. Draw from Woo_2014_NatComms_dpSP_romantic_rejection.pdf. 

1. Load data.  Explanation to draw from in tutorial: The data is first-level condition maps from single subjects, averaging over runs. We define condition maps as task - baseline, where the baseline is the implicit baseline captured by the first-level model intercept. So we have, e.g., single_subject_images_hot, which stores a Hot - Baseline activity map for each participant. Same for the other 3 conditions. Because we have jitter between events, we can estimate the baseline, and thus task - baseline images are stable. In many tasks, baseline is rest. but here it's a visuospatial task, chosen by design because rest could invite periods of rumination about past events (like the picture of one's ex).

Each fmri_data object has subjects in the same order and a .metadata table field with a table object. Explain this briefly.  The metadata table has a row corresponding to each image in the object.  Here it contains a subject ID (one participant per row; subj_id), a condition label ("Condition"), which will make it easy to keep track of conditions later when we stack them for SVM/classifier analysis, and an assignment to cross-validation set or final holdout set ("Orig_partition"), which is convenient for splitting data later in the demo. it is a good idea to have independent test datasets that you test only once on the final model you develop (the "Test" partition here) if you can, to evaluate accuracy, though you need at least a moderately sized study in terms of numbers of participants to do this meaningfully. n = 60 is probably a reasonable minimum.

This is how the data were saved. Don't include this info in the tutorial but you can use it as context.

single_subject_images_hot = enforce_variable_types(all_data_objects.dat_hot);
save DPSP_single_subject_images_hot single_subject_images_hot
single_subject_images_warm = enforce_variable_types(all_data_objects.dat_warm);
save DPSP_single_subject_images_warm single_subject_images_warm
single_subject_images_rejector = enforce_variable_types(all_data_objects.dat_rejector);
save DPSP_single_subject_images_rejector single_subject_images_rejector
single_subject_images_friend = enforce_variable_types(all_data_objects.dat_friend);
save DPSP_single_subject_images_friend single_subject_images_friend

1a. Apply a gray-matter mask (optional).  Apply gray_matter_mask.nii to all the objects here, but note for users that additional non-gray image compartments can be useful for normalization and diagnostics. 

2. Basic group analyses on contrasts

Create fmri_data objects for hot - warm and rejector - friend, subtracting maps for each subject. image_vector.image_math. Each fmri_data object has subjects in the same order and data across all subjects stored in a flat 2-D matrix (obj.dat), which makes it easy to work with. Here just subtracting the matrix allows us to calculate a within-person contrast for each person.

Use ttest to run t-tests on the two sets of contrast maps, and threshold and visualize the resulting images at p < 0.005 uncorrected. Show a montage and surfaces for each using the 'foursurfaces_hcp' option, and label them.

3. Basic SVMs

Explain that decoding includes both classification (predicting categorical labels) and regression (predicting continuous values). We'll use xval_SVM, but there are other options too, fmri_data.predict can run a variety of SVM and regression models.

Now concatenate the hot and warm objects and create a 1 / -1 vector in the resulting object's .Y field for SVM classification with xval_SVM.m. Explain what we are doing and why. We will need the subject id field too in order to make sure we leave out both hot and warm images from the same subject together. This is important so we don't get data leakage and over-optimistic bias in accuracy.

Briefly explain what an SVM is and show the basic equations.
Also briefly explain the CANlab options for constructing holdout sets, e.g., in xval_stratified_holdout_leave_whole_subject_out.m.

Then train the classifier using the standard 10-fold balanced cross-validation (default in xval_SVM) and return a predictive model. Images are X and task condition is Y, with id as grouping variable. Plot the ROC plot using ROC_plot and explain it.  Explain what the cross-validated scores are and where to find them in the predictive model output. Explain how scores, and cross-validated distance from the hyperplane, can be turned into a measure of effect size (Cohen's d) and how it is often more sensitive than accuracy because it is a continuous measure.

Create and plot the confusion matrix using the predictive model object. Explain it.

Construct a preliminary tutorial up to this point and test it.  I will work on some more methods for predictive_model objects and incorporate those into the next part.

 Finish off this part before we go on by building an .mlx called 
  multivariate_classification_with_SVM_part1.mlx
