

-------------
PART 2
-------------
let's make some adjustments before we continue with the tutorial.   Then we can create methods for this object and use them in the tutorial.
a. xval_SVM and other xval_ functions should return a predictive_model class object.



-------------
PART 2
-------------

4. SVM model characterization

Now run through a few of the options that won't take too long to run, and give a brief explanation of what it can do. Cover:
- bootstrapping to get thresholded model weights showing important regions,
- permutation tests to assess the overall significance of classification accuracy and check that permuted values are centered around chance (another check for data leakage and bias),
- repeated cross-validation to get a rough idea of confidence intervals on accuracy (though note these are still optimistically biased and we need better methods for more exact coverage if we are going to interpret P-values on the margin of significance).
- model encoding maps/structure coefficients


5. Cross-classification

Use xval_classify

.......


6.SVM training options

- Normalization and scaling
- Feature selection
- Parameter optimization with nested cross-validation





8. Multi-class classification


9. Parcel-wise


.....

At the end, after the markdown file has been created, turn the whole thing into a live script (.mlx) as well and link to it in the markdown.

Then turn this into a unit test for the SVM analyses and cross-classification.

Create a tweet about this.
